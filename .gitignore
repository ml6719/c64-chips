.fibs/
.vscode/
.DS_Store
CMakeLists.txt
CMake*.json

# Build output, not source - web/index.html is ours and tracked, but the
# compiled .js/.wasm get copied fresh from .fibs/dist/ (see CLAUDE.md's
# build recipe), same convention as machines/atari-st's build-wasm output.
web/c64.js
web/c64.wasm


# Mechanically synced from machines/_shared/src/ by the parent repo's
# build-site.mjs on every build - never hand-edit or commit a copy here.
web/toolbar.css
web/toolbar-dom.js
